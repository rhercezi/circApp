import styled from "@emotion/styled";
import MyButton from "./MyButton";

const DrawerButton = styled(MyButton)({
    textAlign: 'left',
    justifyContent: 'left',
    width: '100%'
});

export default DrawerButton;